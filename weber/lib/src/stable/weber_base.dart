import 'package:weber/weber.dart';

/// Checks if you are awesome. Spoiler: you are.
class WebServer {
  /// Returns a [WebServer].
  WebServer();

  /// Adds a [controller] to the server.
  void addController(ControllerBase controller) {
    // print('Added controller: $controller');
  }

  /// Starts the server.
  Future<void> start() async {
    // print('Server started');
  }
}
